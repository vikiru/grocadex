import { SplashScreen } from '~screens';

export default function Index() {
    return <SplashScreen />;
}

// TODO: frontend tasks, state then backend + final touches
// typography (font size responsiveness, update fonts as needed, logo definetly)
// consistent font use throughout app
// other responsive changes as needed, fix state ui issues
// add expense handling logic + make sure it works as expected
// fix api auth issues, cleanup deps and imports/exports
// add expo/eas.
